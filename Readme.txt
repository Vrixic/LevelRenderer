Contols ->
WASD -> Camera Controls
1 -> main viewport(big screen) -> all viewports camera controls are same
2-> Viewport on left 
3 -> viewport on right

9 -> use normal maps if texturing is supporting on the level
0 -> use texture maps if textureing is supported on the level

f -> swicth level

space -> fly up